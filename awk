awk is a text-processing language designed for pattern scanning and processing. 

----Basic Syntax of Awk#
awk [options] 'pattern { action }' input_file(s)

awk defaults to print $0 (print the entire line) when the pattern matches.
